# Install the CPU build of torch by default. Without this index the plain
# `torch` wheel on PyPI is the CUDA build (~2.5 GB), which is dead weight for
# the CPU inference path the app and the README quickstart describe.
# For a CUDA machine, drop this line and install torch from pytorch.org.
--extra-index-url https://download.pytorch.org/whl/cpu

torch>=2.1.0
torchvision>=0.16.0

# Data & science
numpy>=1.24.0
scikit-learn>=1.3.0
Pillow>=10.0.0
# headless: cv2 is used only for applyColorMap/cvtColor, never for GUI windows.
# The non-headless wheel needs libGL, which CI runners do not have.
opencv-python-headless>=4.8.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# App
streamlit>=1.28.0

# Config
PyYAML>=6.0

# Download
kaggle>=1.5.16

# Dev
pytest>=7.4.0
ruff>=0.5.0
